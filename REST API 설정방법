<REST API 유저단 파일생성방법>
1. 프로젝트를 새로 만든다. 
CREATE NEW STARTER PROJECT
2. 경로 지정
com.petwork
3. lombok, mybatis, thymeleaf, springweb, springboot-devtools
4. dependency 추가
5. 그래들 추가&업데이트
6. 스프링 시큐리티(webConfig, SwaggerConfig, SecurityConfig외2개 추가)
7. Entity 추가
8. XML, MAPPER, SERVICEIMPL, CONTROLLER 추가 
9. 확인
- http://localhost:8091/swagger-ui.html#/index-controller

<RESTAPI MAPPING 방식>
- GetMapping("/select")
- PostMapping("/insert")
- PutMapping("/update")
- DeleteMapping("/delete")
